const store = observable(
{
	route: null, // This is populated by router.js.
	routeParams: null, // This is populated by router.js.
	user: null, // This is populated by Login.jsx.
});

export default store;